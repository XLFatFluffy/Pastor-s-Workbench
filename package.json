{
  "name": "pastors-workbench",
  "private": true,
  "type": "module",
  "version": "0.30.0",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "tauri": "tauri",
    "desktop:dev": "npm install && npm run desktop:prepare && npm exec -- tauri dev",
    "desktop:build": "npm install && npm run desktop:prepare && npm exec -- tauri build",
    "desktop:prepare": "node prepare-desktop.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0"
  },
  "dependencies": {
    "@tauri-apps/plugin-updater": "^2.9.0"
  }
}
